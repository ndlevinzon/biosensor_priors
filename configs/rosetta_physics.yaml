# Stage 2 physics priors — DEPRECATED.
#
# PyRosetta packing/interface scoring was replaced by RoseTTAFold3 docking.
# See configs/rf3_physics.yaml and:
#   python -m biosensor_priors.stage2_physics.wrappers.run_rf3_dock
#
# This file remains only so older docs/links do not 404; it is not loaded.

deprecated: true
replaced_by: configs/rf3_physics.yaml
